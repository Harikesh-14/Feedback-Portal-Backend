export type UserTypes = {
  firstName: string;
  lastName: string;
  email: string;
  phone: number;
  password: string;
}

export type FeedbackDBProps = {
  companyName: string;
  location: string;
  feedback: string;
  rating: number;
}